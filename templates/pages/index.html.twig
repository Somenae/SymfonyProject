{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur notre site de vinyle!{% endblock %}

{% block main %}
<h1>Bienvenue sur notre site de vinyle!</h1>

<form>
        <input type='text' name='product' placeholder='recherchez un produit' value='{{ product }}'>
        <input type='submit' name='submit' value='rechercher'>
</form>
<div class="container">
 <div class="row align-items-center">
    
    
{% for item in products %}
<div class="col-4">
 
    
        <div class="card" style="width: 18rem;">
  <img class="card-img-top" src="..." alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title">{{ item.name }}</h5>
    <h6 class="card-subtitle mb-2 text-muted">{{ item.price }}</h6>
    <p class="card-text">{{ item.description }}</p>
    <a href="" class="btn btn-primary">Acceder à ce produit</a>
  </div>
</div>
</div>
    {% endfor %}
    </div>
    </div>
{% endblock %}
