{% extends 'adminBase.html.twig' %}

{% block title %}Modifier un produit
{% endblock %}

{% block main %}

	<!-- liens -->
	<br>
	<h2>
		<a href="{{ path('app_admin_dashboard') }}">Dashboard</a>
		>
		<a href="{{ path('app_admin_product') }}">Liste des produits</a>
		>
		<a href="{{ path('app_admin_update_product', {'id': product.id}) }}">Modifier
			{{ product.name }}
			{{ product.description }}</a>
	</h2>

	<div class="mt-5">
		<div
			class="col-md-6">

			<!-- card -->
			<div
				class="card">

				{#         <ul class="list-group list-group-flush">
												            <li class="list-group-item">Modifier le vinyle de {{ product.name }} / {{ product.description }} 
												            </li>
												        </ul>
												 #}
				<div class="card-body">
					{{ form_start(form) }}
					{{ form_widget(form) }}

					<br>
					<button type="submit" class="btn btn-primary" name="submit" onclick="return confirm('Voulez-vous vraiment Modifier ?')">Modifier</button>

					{{ form_end(form) }}

					{# 				</ul>#}

					<a href="{{ path('app_admin_product') }}">
						<button type="button" class="btn btn-secondary mt-2">Retour</button>
					</a>
				</div>
			</div>
		</div>
	</div>
	<br>
{% endblock %}
